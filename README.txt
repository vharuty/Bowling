Java version used => java 1.8
Maven version used => Maven 3.3.9
Unit testing framework used => JUnit 4.12

executable file   => bowling\target\Bowling.jar

test cases => Please note that the number of test cases does not indicate the complete coverage. 
		Only a subset is provided for checking main functionality (JUnit framework is used)

User Interface => A simple GUI (java swing) is provided for testing purposes.
		Additional document is not provided with an assumption that GUI is self explanatory.
		Application runs in 2 modes => randon and interactive
			a. Random => the number of pins kicked is generated randomly by game logic
			b. interactive => user can input his/her own number. Pleas not that if the 
				input is not within the desired ranged, game logic generates another input without warrning message. 